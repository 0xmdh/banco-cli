# Publish static site from /landing (e.g. rootstock.wakeuplabs.io)
[build]
  command = "echo 'static'"
  publish = "landing"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
